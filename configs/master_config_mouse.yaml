global:
  node: "mouse-d12"
  sync_regime: "master"
settings:
  poll_timeout_ms: 1000
  sys_metric_update_freq: 15
  log_level: "debug"
temperature:
  sensors:
    - name: "coretemp"
network:
  interface_name: "rteth0"
zmq:
  endpoint: "tcp://armsrv:10000"
  queue_size: 10000
  timeout_after_close_ms: 1000
services:
  ptp4l:
    on: true
    source: "journal"
    dev: "rteth0"
  phc2sys:
    on: true
    source: "journal"
    dev: "rteth0"
  ppswatch:
    on: true
    source: "subproccess"
    dev: "/dev/stand/pps"